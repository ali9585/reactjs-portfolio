import { changePageSection, enableScroll, disableScroll } from "../states/pageSection/pageSectionSlice";

const redux = {
    changePageSection,
    enableScroll,
    disableScroll
};

export default redux;